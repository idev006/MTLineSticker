# Gem A Knowledge Manifest

**Manifest version:** 1.4  
**Fixed attachment budget:** 5 files.  
**Reserved runtime capacity:** 5 files.  
**Platform maximum recorded by Product Owner:** 10 files.  
**Gem Instructions hard limit:** less than 8,000 characters.

> Installer note: `KNOWLEDGE_MANIFEST.md` itself is deployment guidance and is **not** a required Gem knowledge attachment. Put `INSTRUCTION.txt` in the Gem Instructions field, then attach the five fixed knowledge files below.

## Attach these 5 fixed files
1. `01_CORE_GOVERNANCE.txt` — v1.3
2. `02_PRODUCT_COMMUNICATION.txt` — v1.1
3. `03_CHARACTER_STYLE_GOVERNANCE.txt` — v1.3
4. `04_QUALITY_HANDOFF.txt` — v1.2
5. `05_PRODUCTION_PACKAGE.txt` — v2.2

## Runtime/project slots — attach only when relevant
Up to five files, prioritizing:
- current user requirement / Product Owner brief,
- locked or seed caption list when supplied,
- current Character Sheet / Character SSOT when one already exists,
- current Style SSOT / Golden Reference when applicable,
- set-specific source/reference material, including text/image/video references supported by the Gem environment.

A concise Product Owner prompt is expected. Do not require the user to restate internal packaging, QA, anti-invention, source-scope, cardinality, commercial-evaluation, placeholder-prevention or handoff rules already compiled into this Gem.

Keep the Instructions field concise and operational. Put detailed rules in the fixed Knowledge files instead of pushing Instructions toward the platform limit.

Do not waste runtime slots on repository documents already compiled into the fixed pack.

## Loading order
Fixed pack -> establish CURRENT-TASK SOURCE BOUNDARY -> current runtime context -> validate DoR -> execute Product/Commercial Architecture Blackbox -> source-scope/cardinality/commercial pre-package self-audit -> build structured Production Document Package -> Gate A -> handoff.

## Commercial North Star
The goal is not merely documentation completeness or submission readiness. Gem A should maximize the credible chance that the resulting sticker set is useful, distinctive, memorable, repeatedly usable in real chats, and commercially attractive enough to buy, while never claiming guaranteed sales.

## Character Sheet note
Gem A may analyze text/image/video references and create the Character Bible plus Character Sheet specification/generation prompt, but Character Sheet image creation and Product Owner approval occur outside Gem A. `READY_FOR_GEM_B` requires the approved active Character Sheet unless an explicit documented exception exists.

The repository SSOT outranks these compiled deployment files if a conflict is discovered.
